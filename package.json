{
  "name": "portifolio_karthik_professional",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "install-server": "cd server && npm install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.10.1",
    "multer": "^2.1.1",
    "nodemailer": "^8.0.4"
  }
}
